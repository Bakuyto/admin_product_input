# TODO: Debug Invalid JSON and Add Console Print for Insert

## Steps to Complete:
- [x] Add logging for raw $_POST['data'] before JSON decode in add_product.php
- [x] Add logging for JSON decode errors in add_product.php
- [x] Add logging for the SQL INSERT statement before execution in add_product.php
